{
  "type": "object",
  "required": ["name"],
  "properties": {
    "roleId": {
      "description": "ID of the role (this field is required for PUT/PATCH requests)",
      "type": "integer",
      "example": 123
    },
    "role": {
      "description": "Role name",
      "type": "string",
      "example": "administrator"
    },
    "description": {
      "description": "Role description",
      "type": "string",
      "example": "Administrators can do anything, should have all permissions"
    },
    "permissions": {
      "description": "Array of permissions assigned to this role",
      "type": "array",
      "items": {
        "type": "integer"
      },
      "example": [1, 20, 54]
    }
  }
}